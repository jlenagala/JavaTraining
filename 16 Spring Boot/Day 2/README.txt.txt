--------STEPS NOTE--------
Folder-
	path variable - 
			use @pathvariable annotation to get specific employee("/employees/{id}")
	
	data japa and mysql connnector- 
					add those two dependencies 
					appplication.yml file upadated to put the dependencies into class path

					